kind: Module
description: Fake introspect service for Pardot dev env
type: container
name: fake-introspect
image: docker.dev.pardot.com/base/pardot/fake-introspect/app:latest
disabled: ${var.FAKE_INTROSPECT_ENABLED == "false"}
services:
  - name: introspect
    ports:
      - name: node
        containerPort: 8080
include: []

